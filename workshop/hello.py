# We create a variable called "teamName" and store a string in it
teamName = 'IoHeat team'

# We can now print it concatenating strings with the variable
print('Hello, ' + teamName + '!')

# This will do the same using the format() function:
print('Hello again, {}!'.format(teamName) )
